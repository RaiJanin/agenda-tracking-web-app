export function loadCommToEdit(commentId) {
        console.log('Received ID: '+commentId);
    }

    //then, create a variable here to store the information to edit
    //exort the variable to the main module